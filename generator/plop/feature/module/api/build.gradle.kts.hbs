plugins {
    alias(libs.plugins.build.logic.feature.api)
}

kotlin {
    sourceSets {
        commonMain.dependencies {
        }
    }
}

android {
    namespace = "{{packageName}}.feature.{{snakeCase moduleName}}.api"
}
