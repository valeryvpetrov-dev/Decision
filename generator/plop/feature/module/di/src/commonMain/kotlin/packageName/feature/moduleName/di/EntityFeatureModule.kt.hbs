package {{packageName}}.feature.{{snakeCase moduleName}}.di

import org.koin.dsl.module

val {{camelCase entityName}}FeatureModule = module {
    includes(
        {{camelCase entityName}}ImplModule,
        {{camelCase entityName}}PresentationModule
    )
}