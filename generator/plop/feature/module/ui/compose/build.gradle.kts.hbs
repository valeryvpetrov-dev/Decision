plugins {
    alias(libs.plugins.build.logic.feature.ui.compose)
}

kotlin {
    sourceSets {
        commonMain.dependencies {
            implementation(projects.feature.{{camelCase moduleName}}.presentation)
        }
    }
}

android {
    namespace = "{{packageName}}.feature.{{snakeCase moduleName}}.ui.compose"
}
